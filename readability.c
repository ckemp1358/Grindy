// include necessary function libraries
#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int strln(void);
int isalpha(void);

int main(void)
{
    // ask for a string
    string s = get_string("Text: ");
    // create a loop to iterate through the string
    //increase x with each letter
    int x;
    for (int i = 0; i < strln(); i++)
    {
        char c;
        if (isalpha(int c)
        do (x + 1);
        else do(x + 0);
    }

    printf("d%, letters\n", x);
}
